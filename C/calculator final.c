#include<stdio.h>
main()
{
	int a, b, add, diff, prod, div;
	char ch;
	printf("Enter two numbers: ");
	scanf("%d %d",&a,&b);
	printf("Enter the operation to perform between addition, substractio, multiplication or division");
	scanf("%c",&ch);
	switch(A);
	{
		case A:
			add=a+b;
			printf("The addition is %d",&add);
			break;
	}
	switch(S;
	{
		case S:
			diff=a-b;
			printf("The difference is %d",&diff);
			break;
	}
	switch(M);
	{
		case M:
			prod=a*b;
			printf("The product	is %d",prod);
			break;
	}
	switch(D);
	{
		case D:
			div=a/b;
			printf("The quotient is %d",diff);
			break;
	}
	default;
	printf("You haven't entered a valid point.");
}
