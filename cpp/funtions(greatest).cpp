#include<stdio.h>
#include<conio.h>
int greater(int,int,int);
main()
{
int x,y,z,g;
printf("enter the nos");
scanf("%d%d%d",&x,&y,&z);
g=greater(x,y,z);
printf("the greater no is %d",g);
getch(ater(int a,int b,int c)
{
if(a>b&&a>c)
{
return a;
}
if(b>a&&b>c)
{
return b;
}
if(c>a&&c>b)
{
return c;
}
}

